<UserControl x:Class="DragDropSave.DragAndDrop.ItemsToMove"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DragDropSave"
        mc:Ignorable="d"
        Height="450" Width="800"
        x:Name="root"
        >
    <Canvas
        x:Name="canvas"
        AllowDrop="True"
        DragLeave="canvas_DragLeave"
        Drop="canvas_Drop"
        DragOver="canvas_DragOver"
        Background="Transparent"        
        >
        <!--<Rectangle
            x:Name="LabelRectangle"
            Canvas.Left="50"
            Canvas.Top="50"
            Width="50"
            Height="50"
            Fill="{Binding Color,ElementName=root}"
            IsHitTestVisible="{Binding IsChildHitTestVisible,ElementName=root}"
            MouseMove="LabelRectangle_MouseMove"
            />-->
        <!--<local:UserControlJustine IsHitTestVisible="{Binding IsChildHitTestVisible,ElementName=root}"  
            Background="{Binding Color,ElementName=root}"  
            Canvas.Left="200"
            Canvas.Top="200"/>-->
    </Canvas>
</UserControl>
